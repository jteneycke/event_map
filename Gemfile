source 'https://rubygems.org'

# Update Rails
gem 'rails', '5.0.1'
gem 'sqlite3'

#gem 'pg'

gem 'webpacker', github: 'rails/webpacker'

# Maybe a different scheduler?
gem 'whenever', :require => false

gem 'active_model_serializers', git: 'https://github.com/rails-api/active_model_serializers.git', branch: '0-8-stable'

# Maybe we go for some fresh new CSS framework?
gem 'sass-rails', '~> 5.0'
gem 'bootstrap-sass'
gem 'haml-rails'

# Gonna do this up with Vue.js and Webpack
#gem 'therubyracer'
gem 'jquery-rails'
gem 'underscore-rails'
gem 'mustache-js-rails'
gem 'momentjs-rails', '>= 2.9.0'
gem 'bootstrap3-datetimepicker-rails', '~> 4.14.30'
#gem 'uglifier', '>= 1.3.0'
gem 'gmapsjs'

gem 'geocoder'
gem 'pry-rails'

group :production do
  gem 'rails_12factor'
end

group :development, :test do
  gem 'colored'
  gem 'web-console', '~> 2.0'
end
